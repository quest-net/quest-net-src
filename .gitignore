# dependencies
/node_modules

# production
/build
/dist

# misc
.DS_Store
/.claude/settings.local.json
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vite
*.local

# Rust / wasm-pack
# The compiled crate output (wasm/voxel-mesher/pkg) IS committed so the app
# builds without a Rust toolchain; only the intermediate build dir is ignored.
/wasm/voxel-mesher/target
/wasm/target

*.pem
localhost+*.pem
localhost+*-key.pem
IMPLEMENTATION_PLAN.md
AGENTS.md
*.md
